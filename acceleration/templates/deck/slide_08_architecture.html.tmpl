<!--
  Slide 08 — Analysis Pipeline Architecture
  Type: content-slide
  Required tokens: (none; structural diagram. Optional renderer behavior:
                    render_deck.py may inline the contents of
                    templates/mermaid/pipeline_architecture.mmd.tmpl for
                    Rule 4 consistency with the report's diagram.)
  Required non-text visual: Mermaid flowchart in <pre class="mermaid">

  Authority:
    - AAP §0.4.1 (file inventory enumerates this filename)
    - AAP §0.3.1.1 (Analysis Pipeline Architecture Diagram — mirrored here
      in a simplified, executive-readable form per AAP slide-8 Phase 5:
      "the slide should be readable at 1920×1080, so node count is kept
      minimal")
    - AAP §0.7.1 Rule 4 (Visual Architecture Documentation — every diagram
      MUST have a descriptive title and legend; satisfied by the <h3>
      heading and the <p class="diagram-legend"> caption)
    - AAP §0.7.1 Rule 5 (Executive Presentation — content-slide
      constraints: max 4 bullets, ≤40 body words, ≥1 non-text visual,
      zero emoji, no fenced code blocks)
    - AAP §0.7.2.2 Rule 4 (Internal Consistency — this simplified diagram
      remains topologically faithful to the detailed pipeline_architecture
      diagram in acceleration-report.md so the deck and the report tell
      the same architectural story)
    - theme.css §Phase 12 (.diagram-legend styling — compact caption
      below diagrams with a 18px Lucide icon and neutral-500 color)

  Rendering notes:
    - No token substitution is required; the renderer may emit this
      template verbatim into acceleration/executive-presentation.html.
    - Mermaid runtime: the deck initializes Mermaid 11.15.0 with
      startOnLoad:false and calls mermaid.run() on Reveal "ready" and
      "slidechanged" events, so the <pre class="mermaid"> block is
      transformed to inline <svg> at slide-enter time.
    - Lucide runtime: lucide.createIcons() is called on the same events,
      replacing the <i data-lucide="info"> tag with the corresponding
      <svg data-lucide="info"> element; theme.css §Phase 12 targets both
      forms.
    - Tone discipline (Rule 2 — Factual-Neutral): the legend states
      observable facts ("Read-only data flows left to right",
      "metrics.json is the single source of truth"). The forbidden
      subjective-qualifier list in the Boundaries directive does not
      appear in the visible slide body.
    - Body word count outside the <pre>: "Architecture" + "Analysis
      Pipeline" + legend ≈ 21 words, well under the Rule 5 cap of 40.
    - Mermaid syntax: flowchart LR with rectangular process nodes [],
      cylindrical store node [(…)] for metrics.json, and rhombus
      decision node {…} for the verifier outcome — all valid Mermaid
      11.15.0 flowchart shapes.
-->
<section class="content-slide">
  <span class="eyebrow">Architecture</span>
  <h3>Analysis Pipeline</h3>
  <pre class="mermaid">
%%{init: {"flowchart": {"htmlLabels": false, "padding": 18, "nodeSpacing": 35, "rankSpacing": 60, "curve": "linear", "useMaxWidth": true}}}%%
flowchart LR
    G["Git History"] --> E1["Extractors"]
    GH["GitHub API"] --> E1
    CI["CI Artifacts"] --> E1
    E1 --> JL["Normalized JSONL"]
    JL --> C["Classifier + Computer"]
    C --> SoT[("metrics.json")]
    SoT --> R["Report Renderer"]
    SoT --> D["Deck Renderer"]
    SoT --> V["Verifier"]
    R --> Report["acceleration-report.md"]
    D --> Deck["executive-presentation.html"]
    V --> Pass{"Rules 1-6 Pass?"}
  </pre>
  <p class="diagram-legend"><i data-lucide="info" aria-hidden="true"></i> Read-only data flows left to right. metrics.json is the single source of truth consumed by every renderer.</p>
</section>
